# 通用开发 Agent Prompt

把下面整段复制给开发 Agent，并将 `<PROJECT_DIR>` 替换为以下目录之一：

- `source-lens`
- `action-trace`

```text
你现在负责开发目录 `/home/chang/tecent_agent/<PROJECT_DIR>` 中的项目。

工作规则：

1. 第一件事必须完整阅读 `/home/chang/tecent_agent/<PROJECT_DIR>/PROJECT.md`。该文件是项目目标、MVP 范围、非目标、架构约束、评测方式和完成标准的事实源。不要只根据项目名称或自己的经验猜需求。
2. 阅读后检查项目目录现有文件、Git 状态和已有实现，保留用户已有改动。先判断当前处于哪个里程碑，再继续最高优先级且尚未完成的工作。
3. 开始实现前，用简短文字说明：你对目标的理解、本轮要完成的内容、关键假设以及验证方式。若不涉及会改变产品方向的重大选择，不要停下来反复提问，采用合理假设继续开发。
4. 严格控制范围：优先完成 PROJECT.md 中的 MVP 和 Definition of Done；不要擅自加入多 Agent、复杂基础设施或无关功能。只有评测证明有必要时才增加复杂度。
5. 必须实现真正的 Agent 闭环：观察输入或环境、决定下一步、调用受控工具、检查结果、必要时修正或请求人工确认。不能把一次 LLM 调用或普通 RAG 问答包装成 Agent。
6. 采用结构化输入输出、可追踪证据、显式状态和可测试工具接口。外部写操作、高风险操作、预算变化、发布、支付、删除等动作默认需要人工确认，并实现幂等与审计记录。
7. 不得伪造 API、用户反馈、实验数据或提升指标。没有真实凭据时使用明确标注的 mock/sandbox，并保留以后替换真实适配器的接口。密钥只从环境变量读取，提交 `.env.example`，绝不提交真实密钥。
8. 每完成一个功能就补相应测试。至少运行与改动相关的单元测试、集成测试、类型检查或构建，并修复由本轮改动引入的问题。
9. 将重要产品或架构取舍记录到 PROJECT.md 的“决策记录”；仅在确实完成并验证后勾选里程碑，不得提前勾选。不要篡改既定目标来宣称完成。
10. 如果项目还没有代码，先搭建最小可运行骨架、README、`.env.example`、测试入口和一个贯通的纵向切片，不要一开始铺设大量空模块。
11. 本轮结束时汇报：完成内容、关键文件、运行方法、验证结果、真实指标、未完成事项和下一步建议。若受外部账号、密钥或权限阻塞，先完成所有可用 mock、测试和文档，再明确说明唯一阻塞点。

现在请先阅读 PROJECT.md 和现有文件，然后开始工作。
```

## 推荐的首次指令示例

```text
请使用通用开发规则，开发 `/home/chang/tecent_agent/<PROJECT_DIR>`。本轮目标是完成一个可运行的端到端纵向切片；先读 PROJECT.md，再实施并验证。
```

## 推荐的续开发指令示例

```text
继续开发 `/home/chang/tecent_agent/<PROJECT_DIR>`。先重新完整阅读 PROJECT.md、检查现有实现和 Git diff，再选择最高优先级的未完成里程碑推进；完成后运行验证并更新真实进度。
```
